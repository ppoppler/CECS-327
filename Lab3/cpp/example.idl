interface ExampleInterface
{
	string send_message(in string message);
	
	void question_answer(in string question, in string answer);

	string getRandomQuestion();
   	
	string answerQuestion(in string clientAnswer);

	string removeQuestion(in string theIndex);

};
